import {readConfig,normalizeAccession,loadProfile,el,number} from './common.mjs';
const $=id=>document.getElementById(id),palette=['#117663','#318ab5','#8864b4','#ce9250','#ce6387','#597eba','#539c82','#808c52'];
let profile,types,rows,selectedType='',query='',page=0,limit=25,relative=false;
const percent=(v,t)=>t?100*v/t:0;
const display=v=>relative?number(percent(v,profile.total))+'%':number(v);
const color=type=>palette[Math.max(0,types.findIndex(t=>t.name===type))%palette.length];
function renderTypes(){const track=$('composition'),rankings=$('types');track.replaceChildren();rankings.replaceChildren();for(const type of types){const button=el('button');button.style.width=percent(type.value,profile.total)+'%';button.style.background=color(type.name);button.title=type.name+': '+display(type.value);button.setAttribute('aria-label',button.title+'; filter subtypes');button.disabled=!type.value;button.onclick=()=>filterType(type.name);track.append(button);const row=el('button','type-rank'+(selectedType===type.name?' chosen':''));row.setAttribute('aria-pressed',String(selectedType===type.name));const dot=el('span','type-dot');dot.style.background=color(type.name);const bar=el('span','mini-track'),fill=el('span');fill.style.width=percent(type.value,types[0]?.value||1)+'%';fill.style.background=color(type.name);bar.append(fill);row.append(dot,el('span','type-name',type.name),bar,el('b','',display(type.value)));row.onclick=()=>filterType(type.name);rankings.append(row);}}
function filterType(type){selectedType=selectedType===type?'':type;page=0;renderTypes();renderRows();}
function renderRows(){const filtered=rows.filter(r=>(!selectedType||r.type===selectedType)&&r.subtype.toLowerCase().includes(query));const last=Math.max(0,Math.ceil(filtered.length/limit)-1);page=Math.min(page,last);$('subtype-count').textContent=filtered.length+' subtypes';$('measure-heading').textContent=relative?'% OF PROFILE':'ABUNDANCE';$('clear-type').hidden=!selectedType;$('clear-type').textContent=selectedType+' ×';const bars=$('bars');bars.replaceChildren();filtered.slice(page*limit,(page+1)*limit).forEach((r,i)=>{const row=el('div','subtype-bar-row'),label=el('div','subtype-label');label.append(el('b','',r.subtype),el('small','',r.type));const bar=el('div','abundance-track'),fill=el('span');fill.style.width=percent(r.abundance,filtered[0]?.abundance||1)+'%';fill.style.background=color(r.type);bar.append(fill);const value=el('div','abundance-number');value.append(el('b','',display(r.abundance)),el('small','',relative?'abundance '+number(r.abundance):percent(r.abundance,profile.total).toFixed(2)+'% of profile'));row.append(el('span','rank-number',page*limit+i+1),label,bar,value);bars.append(row);});if(!filtered.length)bars.append(el('p','subtype-empty','No subtypes match these filters.'));$('page-label').textContent=`${page+1} / ${last+1}`;$('previous').disabled=page===0;$('next').disabled=page===last;}
function table(){const hasCopy=profile.rows.some(r=>r.copy!==undefined),table=el('table'),head=el('thead'),hr=el('tr');for(const key of ['Type','Subtype',...(hasCopy?['Copy']:[]),'Abundance'])hr.append(el('th','',key));head.append(hr);table.append(head);const body=el('tbody');for(const r of profile.rows){const tr=el('tr');for(const value of [r.type,r.subtype,...(hasCopy?[r.copy??'—']:[]),r.abundance])tr.append(el('td','',value));body.append(tr);}table.append(body);$('data').append(table);}
function tab(id,focus=false){for(const name of ['distribution','data']){const active=name===id;$(name).hidden=!active;$('tab-'+name).setAttribute('aria-selected',String(active));$('tab-'+name).tabIndex=active?0:-1;}if(focus)$('tab-'+id).focus();}
for(const id of ['distribution','data']){$('tab-'+id).onclick=()=>tab(id);$('tab-'+id).onkeydown=e=>{if(['ArrowLeft','ArrowRight','Home','End'].includes(e.key)){e.preventDefault();tab(e.key==='Home'?'distribution':e.key==='End'?'data':id==='data'?'distribution':'data',true);}};}
$('subtype-query').oninput=e=>{query=e.target.value.toLowerCase();page=0;renderRows();};$('measure').onchange=e=>{relative=e.target.value==='relative';renderTypes();renderRows();};$('limit').onchange=e=>{limit=Number(e.target.value);page=0;renderRows();};$('clear-type').onclick=()=>filterType(selectedType);$('previous').onclick=()=>{page--;renderRows();};$('next').onclick=()=>{page++;renderRows();};
$('download').onclick=()=>{const copy=profile.rows.some(r=>r.copy!==undefined);const text=[['type','subtype',...(copy?['copy']:[]),'abundance'].join('\t'),...profile.rows.map(r=>[r.type,r.subtype,...(copy?[r.copy??'']:[]),r.abundance].join('\t'))].join('\n');const url=URL.createObjectURL(new Blob([text],{type:'text/tab-separated-values'}));const link=el('a');link.href=url;link.download=profile.id+'.tsv';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};
try{const accession=normalizeAccession(new URLSearchParams(location.search).get('accession')||'');if(!accession)throw Error('Accession not found. Enter a complete accession on the search page.');profile=await loadProfile(await readConfig(),accession);document.title=accession+' | ARG Atlas';$('profile-title').textContent=accession;const totals=new Map();for(const r of profile.rows)totals.set(r.type,(totals.get(r.type)||0)+r.abundance);types=[...totals].map(([name,value])=>({name,value})).sort((a,b)=>b.value-a.value||a.name.localeCompare(b.name));rows=[...profile.rows].sort((a,b)=>b.abundance-a.abundance||a.subtype.localeCompare(b.subtype));for(const [label,value] of [['Total abundance',number(profile.total)],['Detected types',profile.typeCount],['Detected subtypes',profile.subtypeCount]]){const item=el('span','',label+' ');item.append(el('b','',value));$('metrics').append(item);}for(const [label,value,note] of [['Detected subtypes',profile.subtypeCount,` / ${rows.length} reported`],['Top 3 subtype share',percent(rows.slice(0,3).reduce((s,r)=>s+r.abundance,0),profile.total).toFixed(1),'% of this profile'],['Highest abundance',rows[0]?.subtype||'—','']]){const item=el('div'),valueNode=el('b',typeof value==='string'&&label==='Highest abundance'?'gene-name':'',value);valueNode.append(el('small','',note));item.append(el('span','',label),valueNode);$('insights').append(item);}renderTypes();renderRows();table();$('state').hidden=true;$('profile').hidden=false;}catch(e){$('state').replaceChildren(el('h2','',e.message.startsWith('Accession not found')?'Accession not found':'Unable to load profile'),el('p','',e.message));const retry=el('button','outline-button','Retry');retry.onclick=()=>location.reload();$('state').append(retry);}
